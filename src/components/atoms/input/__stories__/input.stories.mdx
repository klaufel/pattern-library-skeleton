import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { text, boolean } from '@storybook/addon-knobs';
import Input from '..';

export const knobsProps = () => ({
  id: text('id', 'id'),
  name: text('name', 'name'),
  placeholder: text('placeholder', 'Placeholder'),
  disabled: boolean('disabled', false),
  readonly: boolean('readonly', false),
});

<Meta title='Atoms/Input' component={Input} />

# Input

The `Input` component are the text fields that users fill in with different types of information. It’s a field where users can write alphanumeric texts.

<Preview>
  <Story name='Default'>
    <Input />
  </Story>
</Preview>

## Props

<Props of={Input} />

## Stories

### Placeholder

<Preview>
  <Story name='Placeholder'>
    <Input placeholder='Placeholder' />
  </Story>
</Preview>

### Default value

<Preview>
  <Story name='Default value'>
    <Input placeholder='Placeholder' defaultValue='Default value' />
  </Story>
</Preview>

### Disabled

<Preview>
  <Story name='Disabled'>
    <Input placeholder='Placeholder' defaultValue='Default value' disabled />
  </Story>
</Preview>

### Readonly

<Preview>
  <Story name='Readonly'>
    <Input placeholder='Placeholder' defaultValue='Default value' readonly />
  </Story>
</Preview>

### Knobs

<Preview>
  <Story name='Knobs'>
    <Input {...knobsProps()} />
  </Story>
</Preview>
