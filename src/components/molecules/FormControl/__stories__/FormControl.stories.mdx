import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { text, boolean, select } from '@storybook/addon-knobs';
import FormControl from '..';

export const knobsProps = () => ({
  id: text('id', 'form-control'),
  label: text('label', 'Default label'),
  defaultValue: text('defaultValue', ''),
  disabled: boolean('disabled', false),
  status: select('status', { default: '', success: 'success', error: 'error' }, ''),
  formType: select('formType', ['input', 'email', 'phone', 'number', 'textarea', 'password'], ''),
});

<Meta title='Molecules/FormControl' component={FormControl} />

# FormControl

The `FormControl`

<Preview>
  <Story name='Default'>
    <FormControl id='form-control' label='Default label' />
  </Story>
</Preview>

## Props

<Props of={FormControl} />

## Stories

## Knobs

<Preview>
  <Story name='Knobs'>
    <FormControl {...knobsProps()} />
  </Story>
</Preview>
