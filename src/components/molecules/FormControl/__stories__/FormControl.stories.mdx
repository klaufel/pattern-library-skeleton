import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { text, boolean, select } from '@storybook/addon-knobs';
import FormControl from '..';

export const knobsProps = () => ({
  id: text('id', 'form-control'),
  label: text('label', 'Default label'),
  placeholder: text('placeholder', 'Placeholder'),
  status: select('status', { default: '', success: 'success', error: 'error' }, ''),
  disabled: boolean('disabled', false),
  defaultValue: text('defaultValue', ''),
});

<Meta title='Molecules/FormControl' component={FormControl} />

# Input

The `FormControl`

<Preview>
  <Story name='Default'>
    <FormControl label='Default label' />
  </Story>
</Preview>

## Props

<Props of={FormControl} />

## Stories

### Disabled

<Preview>
  <Story name='Disabled'>
    <FormControl label='Default label' disabled />
  </Story>
</Preview>

### Status

<Preview>
  <Story name='Status success'>
    <FormControl label='Default label' status='success' defaultValue='Lorem ipsum dolor' />
  </Story>
  <Story name='Status error'>
    <FormControl label='Default label' status='error' defaultValue='Lorem ipsum dolor' />
  </Story>
</Preview>

## Knobs

<Preview>
  <Story name='Knobs'>
    <FormControl {...knobsProps()} />
  </Story>
</Preview>
