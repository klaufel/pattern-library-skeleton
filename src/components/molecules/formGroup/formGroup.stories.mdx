import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { text, boolean, select } from '@storybook/addon-knobs';
import FormGroup from '.';

export const knobsProps = () => ({
  id: text('id', 'form-group'),
  label: text('label', 'Default label'),
  placeholder: text('placeholder', 'Placeholder'),
  status: select('status', { default: '', success: 'success', error: 'error' }, ''),
  disabled: boolean('disabled', false),
  defaultValue: text('defaultValue', ''),
});

<Meta title='Molecules/FormGroup' component={FormGroup} />

# Input

The `FormGroup`

<Preview>
  <Story name='Default'>
    <FormGroup label='Default label' />
  </Story>
</Preview>

## Props

<Props of={FormGroup} />

## Stories

### Disabled

<Preview>
  <Story name='Disabled'>
    <FormGroup label='Default label' disabled />
  </Story>
</Preview>

### Status

<Preview>
  <Story name='Status success'>
    <FormGroup label='Default label' status='success' defaultValue='Lorem ipsum dolor' />
  </Story>
  <Story name='Status error'>
    <FormGroup label='Default label' status='error' defaultValue='Lorem ipsum dolor' />
  </Story>
</Preview>

## Knobs

<Preview>
  <Story name='Knobs'>
    <FormGroup {...knobsProps()} />
  </Story>
</Preview>
