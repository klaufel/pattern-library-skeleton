import { create } from '@storybook/theming/create';

const theme = create({
  base: 'light',
  brandTitle: 'Patternn library skeleton',
  brandUrl: 'https://github.com/',
  //brandImage: 'your-image-url',
});

export default theme;
